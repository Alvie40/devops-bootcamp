# Study log — FTE 8 semanas

Formato fixo por entrada (vira material de entrevista — "what broke / how I fixed it" são histórias prontas).

---

Date: 2026-08-22
Topic: S1·D1 — Terraform setup e primeiro apply
What I built:
What broke:
How I fixed it:
Interview questions: What is Terraform? · Why use IaC? · What does `terraform plan` do?
Skill status: Terraform C (learning) — dia 1
Next action: S1·D2 — variables, outputs, locals, data sources

---

Date: 2026-09-07
Topic: C++ F0·S1·D1 — trade_stats (RAII, const&, linkage interno)
What I built: CLI `trade_stats` em C++23 — lê CSV `preço,quantidade`, calcula count/min/max/mean/VWAP, conta linhas malformadas sem derrubar. Toolchain do zero: CMake + Ninja, `-Wall -Wextra -Wpedantic -Werror`, ASan/UBSan ligados por padrão, `.clang-format` (Google, ColumnLimit 100). Depois: alvos `format` e `format-check` no CMake.
What broke: (1) clang-tidy acusou `misc-use-internal-linkage` em `max_price` — e o mesmo defeito estava nas outras três funções e no `struct Trade`, o linter só não citou tudo na mesma passada. (2) `which clang-format` volta vazio no macOS e dá a falsa impressão de que falta instalar. (3) Indentação do `min_price` saiu com 4 espaços contra 2 do resto do arquivo — passou despercebido escrevendo à mão.
How I fixed it: (1) `struct Trade` + as quatro funções dentro de um `namespace { }` anônimo, não `static`: `static` não se aplica a tipos e já significa outras três coisas em C++; um bloco só cobre struct e funções e deixa óbvio na leitura que nada ali sai do arquivo. (2) O binário vem com os Command Line Tools mas fora do PATH — `/Library/Developer/CommandLineTools/usr/bin/clang-format`, ou `xcrun -f clang-format`; virou `find_program(... HINTS ...)` no CMake, com `else()` que só avisa em vez de dar FATAL_ERROR, para não travar o build de quem não tem a ferramenta. (3) clang-format no arquivo + alvo `format-check` (`--dry-run -Werror`) para não depender de vista. Conferi os números contra uma implementação independente em awk em vez de conferir o programa com ele mesmo: vwap 103.0656, bate.
Interview questions: What is RAII? (apontando para o `std::ifstream` — abre no construtor, fecha no destrutor, sobrevive ao caminho de exceção via stack unwinding; em C todo `return` antecipado é chance de vazar o handle) · Why pass by `const std::vector<T>&`? (`&` evita copiar o vector inteiro; `const` é promessa verificada pelo compilador de só leitura) · Internal vs external linkage — why does it matter? (símbolo externo abre porta para violação da ODR e bloqueia inlining/DCE, porque o compilador não pode assumir que conhece todos os call sites)
Skill status: C++ C (learning) — dia 1 da Fase 0; toolchain B (o setup está de pé e é reutilizável)
Next action: F0·S1·D2 — referências vs ponteiros, `const`, `std::string_view`. Dívida do dia 1 ainda aberta: nenhum test framework no CMake (o setup da Fase 0 pede Catch2/GoogleTest) e `format-check` não está plugado em hook nem CI — os quatro casos de robustez verificados à mão hoje são os primeiros testes candidatos.

---

Date: 2026-09-08
Topic: C++ F0·S1·D1 (cont.) — Catch2, testabilidade e o custo do linkage interno
What I built: Suíte Catch2 v3 no trade_stats — 10 casos cobrindo parsing (5 formas de linha malformada, stream vazio, só lixo) e estatísticas (gabarito conferido na mão, VWAP≠média com qty diferentes, VWAP=média com qty iguais, trade único, independência de ordem). Para isso a lógica saiu do executável para uma lib (`trade_stats.hpp`/`.cpp` → `trade_stats_lib`) e o `main.cpp` virou só cabeamento de CLI. CMake: `option(FTE_BUILD_TESTS)`, Catch2 via FetchContent com tag fixa, `catch_discover_tests`, warnings movidos para um alvo INTERFACE.
What broke: (1) Não dava para testar nada: função que mora ao lado do `main()`, dentro do executável, não pode ser chamada pelo binário de teste — ele tem o `main()` dele. Obstáculo estrutural, não bug. (2) O namespace anônimo que eu tinha colocado ONTEM virou o impedimento direto: linkage interno é literalmente "ninguém fora deste .cpp me chama". (3) `format-check` falhou no arquivo de teste novo (ordem dos includes) — o alvo que criei ontem pegou meu próprio erro hoje, que é exatamente para isso que ele existe. (4) Descobri, escrevendo teste de caso ruim, que `std::stod`/`std::stol` PARAM no primeiro caractere inválido e não lançam: "101.50abc" vira 101.50 em silêncio e meu parser aceita. Não sabia.
How I fixed it: (1)+(2) Extraí `Trade`, as estatísticas e o parsing para uma lib linkada tanto pelo executável quanto pelos testes; o namespace anônimo saiu. Não é desfazer o commit de ontem — a regra continua "o linkage deve ser o menor que ainda atende os call sites REAIS", e o que mudou foi o requisito, não o critério: o teste virou call site legítimo em outra unidade de tradução. (3) Rodei o alvo `format` e conferi que o `format-check` volta a passar. (4) Escrevi um teste que DOCUMENTA o comportamento atual do stod em vez de escondê-lo — se um dia virar erro explícito, o teste falha e a decisão aparece. Ganho de tabela na extração: `parse_trades` passou a receber `std::istream&` em vez de caminho de arquivo, então o teste alimenta um `istringstream` e não toca em disco — teste que depende de arquivo falha por cwd/permissão e esconde a regressão de verdade. Por fim, não confiei na suíte só porque passou: quebrei o código de propósito três vezes (`<` por `>` no min_price, vwap ignorando a qty, parse sem contar malformada) e cada mutação derrubou o caso certo.
Interview questions: When should a symbol have internal vs external linkage? (o mínimo que atende os call sites reais — e testar É um call site; foi o que me obrigou a abrir o namespace anônimo em 24h) · Why can't you unit-test a function defined next to `main()`? · Should sanitizer flags apply to third-party dependencies? (sim — misturar código instrumentado com não instrumentado dá falso positivo de container-overflow no ASan quando um vector/string cruza a fronteira; já `-Werror` NÃO deve vazar para dependência, senão o build quebra por warning que não é meu — duas flags que andavam juntas e têm escopos opostos) · How do you know your tests actually test anything? (mutation testing: só conta depois que eu vi o teste falhar pelo motivo certo)
Skill status: C++ C+ (learning) — 2 dias de Fase 0; CMake/toolchain B (lib + testes + FetchContent + escopo de flags de pé); testes C (primeira suíte, ainda sem e2e)
Next action: F0·S1·D2 — referências vs ponteiros, `const`, `std::string_view`. Dívida registrada na seção 8 do NOTAS.txt: falta teste end-to-end do binário (argv, exit code, stderr — os 10 casos cobrem lógica, não cabeamento) e `ctest`/`format-check` continuam sem gatilho em hook ou CI. Vector vazio segue como precondição documentada e não testada, por ser UB; a saída real (`std::optional`/`std::expected`) é assunto da S4.

---

Date: 2026-09-17
Topic: DevSecOps crash course — Dia 1 (SAST, secrets, SCA, SBOM)
What I built: `semgrep`/`gitleaks`/`trivy`/`syft` instalados e rodados de verdade contra o próprio repo `devops-bootcamp` (histórico completo + código C/C++/Python) e contra dois arquivos de demonstração no scratchpad — um `.py` com bugs clássicos injetados de propósito e um `requirements.txt` com versões antigas de propósito. Plano completo e resultados em [notes/devsecops-crash-course.md](../notes/devsecops-crash-course.md).
What broke: rodar Semgrep contra o repo real deu 0 findings — não prova nada sobre a ferramenta, só que o código está limpo. Sem um alvo com bug conhecido, não dava pra saber se eu saberia ler um finding de verdade.
How I fixed it: criei `vuln_demo.py` com command injection (`subprocess.call(..., shell=True)`), hash fraco de senha (MD5) e secret hardcoded (chave Stripe fake) — Semgrep pegou os 3, mas **não** pegou um `os.system("tar ... " + filename)` equivalente no mesmo arquivo (mesma classe de bug, padrão sintático diferente). Pra SCA, criei um `requirements.txt` com `requests==2.6.0`/`pyyaml==5.1`/`urllib3==1.24.1` — Trivy achou 20 CVEs reais (3 CRITICAL), inclusive `CVE-2019-20477` (PyYAML RCE via `FullLoader`). Gitleaks no histórico real: 0 leaks (esperado, repo não tem segredo). Syft gerou SBOM do repo com 40 componentes, incluindo GitHub Actions usadas em CI — não só libs de linguagem.
Interview questions: SAST vs DAST vs SCA — uma frase cada · Por que SAST não é oráculo (o `os.system` que escapou é o exemplo vivido) · O que é SBOM e pra que serve quando sai uma CVE nova · Onde secrets scanning entra no fluxo (pre-commit vs CI) · Gitleaks escaneia histórico, não só working tree — por que isso muda a remediação (rotacionar credencial, não só editar o arquivo).
Skill status: SAST (Semgrep) C+ — critérios 1–5 da régua já feitos com demo própria; SCA (Trivy fs) C+ — mesmos critérios; Secrets (Gitleaks) C — só 1–3, falta provocar falha de verdade (leak real) e recuperação; SBOM (Syft) C — só 1–3.
Next action: Dia 2 do crash course — Trivy image contra uma imagem real (achar CVE de base image, trocar por slim/distroless, rescan), OWASP ZAP baseline scan (DAST), wire tudo num workflow único de GitHub Actions com política clara de blocking vs non-blocking, e `notes/devsecops-interview.md` com as perguntas cronometradas.

---

Date: 2026-09-17
Topic: DevSecOps crash course — Dia 2 (container scan, DAST, pipeline)
What I built: app FastAPI mínima em [python/devsecops-lab/](../python/devsecops-lab/) (alvo real, só o suficiente pra escanear/rodar) com dois Dockerfiles (`Dockerfile` = `python:3.11-slim`, `Dockerfile.full` = `python:3.11` completo, mantido só pra comparação). Buildei os dois, escaneei com Trivy, subi a app com `docker run` e rodei OWASP ZAP baseline scan (DAST) contra ela de fora. Wire completo em [.github/workflows/devsecops-lab.yml](../.github/workflows/devsecops-lab.yml): SAST+secrets+SCA em paralelo (bloqueantes) → build+container scan (bloqueante) → SBOM (artefato) + DAST (só reporta) em paralelo. Perguntas e régua C→B em [notes/devsecops-interview.md](../notes/devsecops-interview.md).
What broke: Docker Desktop não estava rodando (precisei abrir e esperar o daemon subir) antes de conseguir buildar qualquer imagem.
How I fixed it: `open -a Docker` + polling em `docker info` até o daemon responder. Trivial, mas é o tipo de coisa que trava um pipeline de CI se a máquina/runner não tiver o daemon pronto — motivo de sempre ter um passo de "wait for docker" ou usar um runner que já vem com Docker ativo.
Interview questions: Por que escanear a imagem além do código? (respondida com número real: 649 vulnerabilidades CRITICAL/HIGH no `python:3.11` completo → 57 no `-slim`, queda de 91%, sem tocar em uma linha de app) · O que DAST pega que SAST não pega, e vice-versa (ZAP achou 3 headers de runtime ausentes que não existem lendo o `app.py` de 12 linhas; ZAP nunca acharia o command injection do dia 1 porque não ia adivinhar o input certo) · Blocking vs non-blocking gate — como decidir (código/dependência própria sempre bloqueia; DAST só reporta até existir triagem).
Skill status: SAST/SCA/container scan (Semgrep/Trivy) **B** — passaram pelos 8 critérios da régua com demo própria (falha provocada, diagnosticada, tradeoff explicado). SBOM (Syft) e DAST (ZAP) **C+** — falta consumir o SBOM numa CVE real e configurar scan autenticado no ZAP. Secrets (Gitleaks) segue **C** — nunca vivi um leak real.
Next action: fora do crash course por ora — próximos gaps ficam pra pauta de reforço se aparecerem de novo no funil do job-bot (`ROADMAP-FTE.md` § job-bot). Se sobrar tempo antes de uma entrevista técnica: praticar rotação de credencial de verdade (simular leak → revoke → reemitir) e um scan ZAP autenticado.

---

Date: 2026-09-18
Topic: DevSecOps crash course — Dia 1 refeito na mão + mitigação real (SCA, container, DAST)
What I built: refiz o Dia 1 inteiro digitando os comandos eu mesmo (não só observando) em `~/devsecops-practice/`: Gitleaks contra o histórico do repo, Semgrep com a regra custom de novo, Trivy fs, Syft. Depois fui além do "achar" pra "corrigir e reverificar": subi `requests`/`pyyaml`/`urllib3` pra versão fixa segura, atualizei `fastapi` em `python/devsecops-lab/requirements.txt` (o que puxou um `starlette` sem CVE), adicionei middleware de security headers em `app.py`, e rescaneei tudo de novo pra provar. Consolidado em [notes/devsecops-scan-report.md](../notes/devsecops-scan-report.md).
What broke: (1) tentei corrigir o SCA usando `requests>=2.33.0` (range) — o Trivy simplesmente não escaneou (`num=0`), porque ele só lê versão pinada. (2) tentei fechar as 2 CVEs residuais do container (`jaraco.context`, `wheel`) rodando `pip install --upgrade pip setuptools wheel` antes do resto — o total de vulnerabilidades **não mudou** (59→59), só trocou de pacote: sumiu `jaraco.context`/`wheel`, apareceu `setuptools CVE-2025-47273` e `msgpack GHSA-6v7p-g79w-8964`, ambos vendorizados dentro do próprio pip.
How I fixed it: (1) troquei `>=` por `==` fixado na versão corrigida — Trivy voltou a escanear e deu 0. Lição: SCA em CI exige manifesto pinado/lockado, range solto deixa a ferramenta cega. (2) revertido — a "correção" não tinha ganho líquido e ainda adicionava complexidade (pip como root) por nada; documentei como residual aceito em vez de forçar. Prova de que "corrigir" sem reverificar é perigoso: eu quase reportei isso como resolvido antes de rodar o Trivy de novo e ver que trocou um CVE por outro.
Interview questions: Por que um range (`>=`) quebra SCA automatizado, mesmo resolvendo o problema de segurança na prática? · O que fazer quando "corrigir" uma dependência troca um CVE por outro — como isso mudou o approach (sempre reverificar, nunca assumir upgrade = menos risco) · Por que `Cache-Control: no-store` fez o ZAP trocar um WARN por outro diferente (efeito colateral esperado, não bug).
Skill status: SCA (Trivy fs) sobe pra **B+** — corrigiu de verdade e reverificou (20→0). Container scan mantém **B**, mas agora com um caso real de "tentativa que não ajudou" documentado (sinal de maturidade maior que só o caso feliz). DAST sobe pra **B** — resolveu 2 dos 3 achados com fix real, e justificou o residual em vez de ignorar.
Next action: se sobrar tempo, o item que falta de verdade pro crash course inteiro é secrets (rotação de credencial real) — segue em C+ até acontecer um leak de verdade pra praticar em cima.

---

Date: 2026-09-18
Topic: DevSecOps — fechando pendências reais (pre-commit hook, Dependabot, distroless, consumo de SBOM)
What I built: fui item por item da lista de "pendente" do crash course e resolvi o que dava pra resolver sem inflar escopo. (1) `.pre-commit-config.yaml` na raiz com o hook oficial do Gitleaks + `pre-commit install`. (2) `.github/dependabot.yml` cobrindo pip/`requirements.txt`, a base image do Dockerfile e as GitHub Actions do repo. (3) `python/devsecops-lab/Dockerfile.distroless` — multi-stage build (`python:3.11-slim` como builder, `gcr.io/distroless/python3-debian12` como runtime) — e um trigger `schedule` no workflow (segunda 06:00 UTC) pra pegar patch de base image sem depender de push. (4) exercício real de consumir SBOM: simulei CVE em `anyio` e consultei o `sbom.json` já gerado em vez de re-escanear.
What broke: (1) testei o hook do Gitleaks com `AKIAIOSFODNN7EXAMPLE` esperando bloqueio e ele **passou** — quase reportei "hook não funciona" antes de investigar. (2) a imagem `distroless` roda sem shell — não dava pra fazer `docker exec sh` pra verificar se subiu certo, precisei confiar em `curl` de fora + `docker logs`.
How I fixed it: (1) descobri que `AKIAIOSFODNN7EXAMPLE` é o placeholder oficial da doc da AWS e está numa allowlist padrão do Gitleaks — refiz o teste com uma chave `AKIA…` gerada aleatoriamente (valor não reproduzido aqui de propósito) e o hook bloqueou de verdade (`exit code 1`). Se eu não tivesse desconfiado do resultado "bom demais", teria concluído errado que o hook não funciona. (2) `curl -sI`/`curl -s` de fora do container + `docker logs devsecops-lab-run` confirmaram app no ar com os headers de segurança intactos — validação sem depender de shell dentro da imagem, que é exatamente a limitação operacional que distroless impõe em produção.
Interview questions: Por que uma allowlist padrão pode mascarar um teste malfeito de ferramenta de segurança — como evitar essa armadilha (nunca testar com o exemplo "de canhão" de um vendor) · Distroless: o que se ganha e o que se perde (superfície de ataque menor vs. sem `docker exec sh` pra debug) · Por que rebuild agendado (cron) é necessário mesmo com a mesma tag de base image pinada (patch de segurança pode mudar o conteúdo da tag sem mudar o nome).
Skill status: Secrets (Gitleaks) sobe pra **B** — hook real instalado e testado nos dois sentidos (bloqueou de verdade + achou o gap da allowlist). Container scan sobe pra **B+** — 59→49 com distroless, e os 2 CRITICAL residuais confirmados como "sem fix upstream ainda" (não gap de esforço). SBOM sobe pra **B** — pratiquei o consumo, não só a geração.
Next action: os dois itens que seguem genuinamente fora de alcance sem inflar escopo: rotação de credencial real (precisa de um leak de produção de verdade) e scan autenticado no ZAP (a app não tem autenticação — adicionar só pra isso seria escopo inflado). Ambos ficam registrados como pendência honesta, não como "não pensei nisso".
